# Production Environment Variables
# DO NOT commit your actual production .env.production file to git
# Add .env.production to .gitignore

# Application URL (your production domain)
NEXT_PUBLIC_APP_URL=https://your-domain.com

# Supabase Cloud (Production)
# Get these from: https://app.supabase.com/project/YOUR_PROJECT/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-production-anon-key-here

# Service role key (server-side only, NEVER expose to client)
SUPABASE_SERVICE_ROLE_KEY=your-production-service-role-key-here

# Migration API Access (for applying migrations to production)
# Get access token from: Supabase Dashboard → Account → Access Tokens
# ⚠️ Store these in your CI/CD secrets, NOT in committed files
SUPABASE_ACCESS_TOKEN=your-production-management-api-token
SUPABASE_PROJECT_REF=your-production-project-reference

# Database Connection URL (optional, for direct connections)
# Get from: Supabase Dashboard → Settings → Database → Connection String
DATABASE_URL=postgresql://postgres.[project_ref]:[password]@[host]:5432/postgres

# IMPORTANT SECURITY NOTES:
# 1. Never commit this file with real values to git
# 2. Use environment variables in Vercel/Netlify/deployment platform
# 3. Service role key has admin access - keep it secure
# 4. Rotate all keys immediately if they are ever exposed
# 5. Use separate tokens for production vs. development
# 6. Restrict token permissions to only what's needed
