# Production Environment Variables
# DO NOT commit your actual production .env.production file to git
# Add .env.production to .gitignore

# Application URL (your production domain)
NEXT_PUBLIC_APP_URL=https://your-domain.com

# Supabase Cloud (Production)
# Get these from: https://app.supabase.com/project/YOUR_PROJECT/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-production-anon-key-here

# Optional: Service role key (server-side only, NEVER expose to client)
# SUPABASE_SERVICE_ROLE_KEY=your-production-service-role-key-here

# IMPORTANT SECURITY NOTES:
# 1. Never commit this file with real values to git
# 2. Use environment variables in Vercel/production deployment
# 3. Service role key has admin access - keep it secure
# 4. Rotate keys if they are ever exposed
